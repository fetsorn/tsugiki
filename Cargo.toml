[package]
name = "tsugiki"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
markdown = "1.0.0"
unicode-segmentation = "1.13.2"
uuid = { version = "1", features = ["v4", "v5"] }

[dev-dependencies]
tempfile = "3"
